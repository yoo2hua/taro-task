// import store from '@/store'
// import { userThunks } from '@/store/modules/user'
import { $ } from './$'

export default async function setup() {
  console.log('🌊 ~ file: setup.ts:8 ~ setup ~ $:', $)
  // store.dispatch(userThunks.login()).then(() => {
  // })
}
